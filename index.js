
   let input = prompt("Ведите любимое число")
    inputNum = parseInt(input, 10) {    

     if (isNaN(inputNum) || inputNum < 1)
    alert("Вы ввели не правильное число")
    
    } else {
    let a (input * input)-1)%24;
    if (input === 1 || input === 2 || input === 5 || input === 25);
    else if 
}

